"""Offline evaluation / optimization pilots (not shipped in the wheel).

Repo-root tooling for measuring and improving mimir's prompts against datasets
or rubrics. Kept out of the ``mimir`` package so it never bloats the runtime.
"""
